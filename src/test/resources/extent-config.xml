<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
    <configuration>
        <!-- report theme -->
        <theme>dark</theme>

        <!-- document encoding -->
        <encoding>UTF-8</encoding>

        <!-- protocol for script and stylesheets -->
        <protocol>https</protocol>

        <!-- title of the document -->
        <documentTitle>Test Automation Report</documentTitle>

        <!-- report name - displayed at top-nav -->
        <reportName>Automation Test Results</reportName>

        <!-- timestamp format -->
        <timeStampFormat>MMM dd, yyyy HH:mm:ss</timeStampFormat>

        <!-- custom javascript -->
        <scripts>
            <![CDATA[
                $(document).ready(function() {
                    // Rapor yüklendiğinde çalışacak özel JavaScript kodları
                    $('.brand-logo').text('CatchyLabs Test Report');
                    $('body').addClass('dark');
                });
            ]]>
        </scripts>

        <!-- custom styles -->
        <styles>
            <![CDATA[
                .nav-wrapper { background-color: #1E242C !important; }
                .brand-logo { margin: 0 10px; }
                .report-name { margin-left: 10px; }
                .blue.darken-3 { background-color: #1E242C !important; }
                .side-nav { background-color: #2C333D; }
                .side-nav li.active { background-color: #1E242C; }
                .dashboard-view { background-color: #2C333D; }
                .dashboard-view .panel { background-color: #1E242C; }
            ]]>
        </styles>
    </configuration>
</extentreports>